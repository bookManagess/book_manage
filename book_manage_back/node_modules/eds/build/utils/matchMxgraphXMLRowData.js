module.exports = function matchMxgraphXMLRowData(xml) {
  const regex = /<diagram .*?>(.*?)<\/diagram>/g;
  let match = regex.exec(xml);
  const array = [];
  while (match != null) {
    array.push(match[1]);
    match = regex.exec(xml);
  }
  return array;
}